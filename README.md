# SCRAPPER built in KROWDY bootcamp

* Instalar dependencias:
```bash
    yarn
```

* Iniciar proyecto: 
```bash
    yarn dev
```
* Copiar manifest.json en la carpeta dist (iniciar el proyecto antes)

* Cargar la carpeta dist como extension en tu navegador